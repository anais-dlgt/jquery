$(document).ready(function() {
    $('#show').empty();
    $('.<%= email.id %>').html("<%= j (render email) %>");
    $(document).on('turbolinks:load', function(){
        alert('Message supprimé !');
});
