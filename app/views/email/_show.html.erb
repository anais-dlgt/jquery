  <h2><%= @email.object %></h2>
  <p><%= @email.body %></p>
  <hr>
  <button class="delete btn btn-danger" data-id="<%=@email.id%>"><%= link_to 'Supprimer', email_path(@email.id), :method => :delete, data: { confirm: "En es-tu sûr ?"}, :remote => true, :class => 'delete-link'%></button>
